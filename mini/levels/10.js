function setupLevel() {
  platformWidth = 80;
  platformHeight = 20;
  platformColor = "orange";

  createPlatform(40, 100, platformWidth, platformHeight, platformColor);
  
  //Use the VARIABLES platformWidth, platformHeight and platformColor to make two new platforms. Refer to the code above.

}

