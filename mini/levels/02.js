function setupLevel() {
//The first two numbers control the position, the last two numbers control the size.
//Example (x-position, y-position, width, height);
//INSTRUCTIONS:  Change the size of the platforms below so you can reach the goal

//TIP The level has a WIDTH of 640 and a HEIGHT of 480
  createPlatform(40, 100, 80, 10);
  createPlatform(60, 180, 80, 10);
  createPlatform(510, 400, 80, 10);
}
