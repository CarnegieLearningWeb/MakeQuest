function setupLevel (){

  //Update platform1 to be one unit to the right of its original position. Change the number 30 to 1.
  platform1.position.x = platform1.position.x + 30;
}

function drawLevel (){

  //Update platform1 to be one unit to the right of its original position. 
  platform2.position.x = platform2.position.x - 1;
}